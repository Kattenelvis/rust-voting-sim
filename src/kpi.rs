pub mod kpi {
    // A KPI can be a generic <T> so long as it implements a maximum and minimum value (So a
    // partial ordering?)
    // So long as it's a total ordering... no let's make it more general
    // It needs to implement
    // uhhhh
    // It can definetly be any signed or unsigned integer
    // any enum if there's a match case for matchigng each case to a value.

    pub fn main() -> u128 {
        // let a: u128 = 9 * 10_u128.pow(1000000000);
        // a

        1
    }
}
